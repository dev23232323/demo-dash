"use client";
import styled from "styled-components";

export const StyledBannerToolbar = styled.div`
  position: absolute;
  display: flex;
  top: 10px;
  right: 10px;
  gap: 5px;
`;

export const StyledBannerToolbarWrapper = styled.div`
  position: relative;
`;
