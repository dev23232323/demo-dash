"use client";
import React, { FC } from "react";

interface WorkPageProps {}
const WorkPage: FC<WorkPageProps> = ({}) => {
  return <div>Todo: list all the works here</div>;
};
export default WorkPage;
